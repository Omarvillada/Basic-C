#include <stdio.h>
#include <stdlib.h>

int main()
{
    int rSum;
    float rRes;
    float rDiv;
    float rMult;
    int rMod;

    rSum = 7 + 3;
    rRes = 7.1 - 3.1;
    rDiv = 7.0 / 3.0;
    rMult = 7 * 3;
    rMod = 7%3;

    printf("Rsultado de la suma: %i \n", rSum);
    printf("Rsultado de la resta: %f \n", rRes);
    printf("Rsultado de la division: %f \n", rDiv);
    printf("Rsultado de la multiplicacion: %f \n", rMult);
    printf("Rsultado del modulo es: %i \n", rMod);
    return 0;

    //Reto 1
    //Ingresar los valores necesarios para calcular el area de un cilindro
    //Realizar operacio
    //Imprimer el resultado del area

    //Reto 2
    //Ingresar la temperatura en grados F
    //Realizar la operacion para convertir de grados F a C
    //Imprimer el resultado de la conversion


}
